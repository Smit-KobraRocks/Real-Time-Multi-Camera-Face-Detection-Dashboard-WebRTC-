package data

// Cascade returns nil data in the stub implementation. Replace this package
// with the upstream dependency to enable the real detector.
func Cascade() ([]byte, error) {
	return nil, nil
}
